app-id: life.bolls.bolls
base: org.electronjs.Electron2.BaseApp
base-version: '21.08'
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
command: bolls-run
separate-locales: false
tags: []
finish-args:
  - --socket=x11
  - --share=ipc
  - --share=network
  - --device=dri
  - --filesystem=~/.config
modules:
  - name: unappimage
    buildsystem: simple
    build-commands:
      - make -C squashfs-tools -j ${FLATPAK_BUILDER_N_JOBS} install INSTALL_DIR=/app/bin
    sources:
      - type: git
        url: https://github.com/refi64/unappimage
        commit: d7f86f2a0d7ec3a69211125207d5f127386b849a

  - name: bolls
    buildsystem: simple
    build-commands:
      - unappimage bolls-0.2.1.AppImage
      - rm bolls-0.2.1.AppImage
      - mv squashfs-root /app/bin/bolls
      - install -D bolls-run -t /app/bin
      - install -Dm644 life.bolls.bolls.appdata.xml -t /app/share/metainfo
      - install -Dm644 life.bolls.bolls.desktop -t /app/share/applications
      - install -Dm644 life.bolls.bolls.svg -t /app/share/icons/hicolor/128x128/apps

    sources:
      - type: script
        dest-filename: bolls-run
        commands:
          - zypak-wrapper /app/bin/bolls/bolls "$@"
      - type: file
        path: life.bolls.bolls.desktop
      - type: file
        path: life.bolls.bolls.svg
      - type: file
        path: life.bolls.bolls.appdata.xml
      - type: file
        filename: bolls-0.2.1.AppImage
        url: https://storage.googleapis.com/resurrecting-cat.appspot.com/Bolls-0.2.1.AppImage
        sha256: f976c38c5ca0f10bbae88297d1ff5341e54f487237333859220346c1979ed80b
        size: 83987531
      - type: shell
        commands:
          - sed -i '3i <releases><release version="2.1.78" date="2023-03-01"></release></releases>'
            life.bolls.bolls.appdata.xml
